VS code - Terminal

pip install flask
pip show flask

---Create--

mkdir flask_app
cd flask_app

code .

3. app.py

from flask import Flask

# Create a Flask application
app = Flask(__name__)

# Define a route
@app.route('/')
def home():
    return '<h1 style="color:blue; text-align:center;">🚀 Welcome to My First Flask App!</h1>'

# Run the app
if __name__ == '__main__':
    app.run(debug=True)

4. python app.py

5. https.......

--------------------


app.py

from flask import Flask, render_template_string

app = Flask(__name__)

HTML_PAGE = """
<!DOCTYPE html>
<html>
<head>
  <title>Flask Demo</title>
  <style>
    body {
      background: linear-gradient(to right, #ff758c, #ff7eb3);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: white;
      text-align: center;
      padding-top: 100px;
    }
    .card {
      background: rgba(255, 255, 255, 0.2);
      border-radius: 15px;
      width: 50%;
      margin: auto;
      padding: 30px;
      box-shadow: 0 0 20px rgba(0,0,0,0.3);
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>🚀 Welcome to My First Flask App!</h1>
    <p>This app is built and running directly from VS Code.</p>
  </div>
</body>
</html>
"""

@app.route('/')
def home():
    return render_template_string(HTML_PAGE)

if __name__ == '__main__':
    app.run(debug=True)
4. requirements.txt
Flask==2.2.5

--->  pip install -r requirements.txt

5. Dockerfile

# Use a slim Python base image
FROM python:3.9-slim

# Improve logging & disable bytecode files
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# Copy requirements and install first for caching
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy app code
COPY . .

# Expose the port Flask listens on
EXPOSE 5000

# Run the app
CMD ["python", "app.py"]



6.
docker build -t flask-docker-app:latest .

docker images | grep flask-docker-app

docker run --name flaskapp -p 5000:5000 -d flask-docker-app:latest

docker logs -f flaskapp

docker login
kpneha4@gmail.com
kpneha@1234

docker tag flask-docker-app:latest kpneha4/flask-docker-app:latest

docker push kpneha4/flask-docker-app:latest
---
docker pull kpneha4/flask-docker-app:latest
docker run -d --name flaskapp -p 5050:5000 kpneha4/flask-docker-app:latest

